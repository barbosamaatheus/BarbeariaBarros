<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.barbosa.myapplication">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:configChanges="orientation"
        android:icon="@mipmap/ic_icone"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_icone_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyTheme">
        <activity android:name=".Activitys.SplashActivity">
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activitys.MainActivity" />
        <activity android:name=".Activitys.SobreActivity" />
        <activity android:name=".Activitys.ReservaActivity" />
        <activity android:name=".Activitys.ServicosActivity" />
        <activity android:name=".Activitys.CamActivity" />
        <activity android:name=".Activitys.GalleryActivity" />
        <activity android:name=".Activitys.LoginActivity" />
        <activity android:name=".Activitys.CadastroActivity"></activity>
    </application>

</manifest>